# Synthetic Formation end-to-end task

Plan a tiny pure Python clamp(value, lower, upper) function in clamp.py.
Return lower when value is below lower, upper when above upper, otherwise value.
Raise ValueError when lower > upper. Preserve numeric type when returning a bound.
No I/O, dependencies, or unrelated files. The Codex host owns apply and test gates.
Qwen will implement from your plan; you will independently review it afterwards.
Inspect this workspace through the bound connector. Give concrete implementation
guidance and boundary tests. No requirement to change anything outside this fixture.
